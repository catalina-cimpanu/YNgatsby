import React from "react";
import { Link } from "gatsby";
import styled from "styled-components";

const PreviousNextButtons = () => {
  return <div>PreviousNextButtons</div>;
};

export default PreviousNextButtons;
